#include<bits/stdc++.h>
using namespace std ;

int main()
{
    int t ;
    cin >> t ;
    while(t--)
    {
        int n ;   // no. of villagers
        int k ;   // no. of groups

        cin >> n >> k ;

        vector<int> a(n) ;
        for(int i = 0 ; i < n ; ++i)
        {
            cin >> a[i] ;
        }
        int power = 0 ;
        if(n==1)
        {
              power = 0 ;
        }

        int count = 0 ;
        
        for(int i = 0 ; i < n ; ++i)
        {

        }
    }
}